<h2 class="text-center mb-4">会員一覧</h2>
<p>会員検索</p>
<%= form_with url: admin_members_search_path, method: :get do |f| %>
  <%= f.text_field :word %>
  <%= f.submit '検索', class: 'btn btn-success' %>
<% end %>
<%= render 'admin/members/search_members', members: @members %>