<div class = 'container'>
  <div class = 'row justify-content-center'>
    <div class = 'col-md-6 col-12 text-center'>
      <%= image_tag @group.get_group_image %>
      <h2 class = 'text-center mb-4'><%= @group.name %></h2>
      <h1 class = 'text-center mb-4'><%= @group.owner_id %></h1>
      <p>グループポストの一覧（古い順から）<br>
        新規・編集  グループポストのフォーム<br>
        各ポストにいいね、コメント<br>
        自分のポストだけコメント見れる<br>
      </p>
      <% if current_member.active_memberships.where(group_id: @group.id).exists? %>
        <%= link_to '退会する', group_membership_path(@group.id, @membership.id), method: :patch, data: { confirm: "本当に退会しますか？" }, class: 'btn btn-secondary' %>
      <% end %>
      <% if @group.owner.id == current_member.id %>
        <%= link_to '編集する', edit_group_path(@group.id), class: 'btn btn-secondary' %>
      <% end %>
    </div>
  </div>
</div>
