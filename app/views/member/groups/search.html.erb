<h2 class="text-center mb-4">検索結果</h2>

<div class = 'row justify-content-center'>
<% @search_groups.each do |group| %>
  <div class = 'col-6 col-md-4 mb-4 col-lg-3 text-center'>
    <%= link_to 'グループの詳細リンク・画像も貼りたい', infomation_path(group), class: 'd-block' %>
    <%= group.name %>
  </div>
<% end %>
</div>